# No Duplicate Experiements

The Snake Lab runs a professional environment. Simulations are deterministic. Re-running a simulation will produce the same results.

THIS SIMULATION HAS BEEN RUN!

Try increasing or decreasing your choice by a small factor, but make sure that new LR has not already been tested.

Submit your new choice using `submit_learning_rate`. Supply only `learning_rate`, a finite number greater than zero and at most one. A prose answer does not submit an LR. Report data is JSON; null means unavailable, not zero.