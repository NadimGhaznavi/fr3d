# No Duplicate Experiments

The Snake Lab runs a professional environment. Simulations are deterministic. Re-running a simulation will produce the same results.

THIS SIMULATION HAS BEEN RUN!

Try increasing or decreasing your choice by a small factor, but make sure that new epsilon decay has not already been tested with learning rate `0.00021`.

Submit your new choice using `submit_epsilon_decay`. Supply only `epsilon_decay`, a finite number greater than zero and at most one. A prose answer does not submit a value. Report data is JSON; null means unavailable, not zero.
