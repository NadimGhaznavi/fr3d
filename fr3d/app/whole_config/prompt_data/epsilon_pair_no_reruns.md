# No Epsilon Pair Reruns

The complete proposed configuration already exists. Choose another unused epsilon pair within the supplied bounds, holding all other active-baseline settings fixed. Call `submit_epsilon_pair` with only `initial` and `decay`, supplying both values again. No value from the rejected pair has been retained.
