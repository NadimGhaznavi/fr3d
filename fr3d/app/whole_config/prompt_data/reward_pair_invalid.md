# Invalid Reward Pair

Correct the stated validation error and call `submit_reward_pair` with only `closer_to_food` and `further_from_food`. Supply both values together, choosing one eligible pair from the report. At least one value must differ from the active baseline. All other baseline settings remain fixed. No value from the rejected pair has been retained.
