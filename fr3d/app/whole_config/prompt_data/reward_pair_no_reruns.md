# No Reward Pair Reruns

The complete proposed configuration already exists. Choose another eligible reward pair, holding all other active-baseline settings fixed. Call `submit_reward_pair` with only `closer_to_food` and `further_from_food`, supplying both values again. No value from the rejected pair has been retained.
