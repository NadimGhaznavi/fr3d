# Invalid Epsilon Pair

Correct the stated validation error and call `submit_epsilon_pair` with only `initial` and `decay`. Supply both values together, within the supplied bounds. At least one value must differ from the active baseline. All other baseline settings remain fixed. No value from the rejected pair has been retained.
